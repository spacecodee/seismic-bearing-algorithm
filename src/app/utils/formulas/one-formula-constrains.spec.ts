import { OneFormulaConstrains } from './one-formula-constrains';

describe('OneFormulaConstrains', () => {
  it('should create an instance', () => {
    expect(new OneFormulaConstrains()).toBeTruthy();
  });
});
