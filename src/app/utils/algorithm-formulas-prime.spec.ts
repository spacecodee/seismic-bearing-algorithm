import { AlgorithmFormulasPrime } from './algorithm-formulas-prime';

describe('AlgorithmFormulasPrime', () => {
  it('should create an instance', () => {
    expect(new AlgorithmFormulasPrime()).toBeTruthy();
  });
});
