import { ExponentialFormulas } from './exponential-formulas';

describe('ExponentialFormulas', () => {
  it('should create an instance', () => {
    expect(new ExponentialFormulas()).toBeTruthy();
  });
});
